[project]
name = "rpc-municipal"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "duckdb",
    "marimo",
    "pandas",
    "plotly",
    "scipy",
]
[tool.marimo.runtime]
output_max_bytes = 50_000_000
